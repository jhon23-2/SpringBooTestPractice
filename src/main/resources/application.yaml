spring:
  application:
    name: Test
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: testuser
    password: testpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # or "none" when using SQL scripts
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
